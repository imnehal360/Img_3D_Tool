# Image-to-3D / original Microsoft TRELLIS environment
#
# Tested with Python 3.10.20 on Linux and the environment used by this
# project. Python 3.12+ is intentionally unsupported by the TRELLIS setup.
# A CUDA-capable NVIDIA driver and a GPU with about 16 GB VRAM are still
# required; pip cannot install the host NVIDIA driver.

--extra-index-url https://download.pytorch.org/whl/cu121

# PyTorch 2.4.0 + CUDA 12.1 (keep these versions in sync)
torch==2.4.0+cu121
torchvision==0.19.0+cu121
torchaudio==2.4.0+cu121

# TRELLIS runtime and native-extension prerequisites
# Kaolin is hosted by NVIDIA, not PyPI; this wheel index matches PyTorch 2.4.0 + CUDA 12.1.
--find-links https://nvidia-kaolin.s3.us-east-2.amazonaws.com/torch-2.4.0_cu121.html
addict==2.4.0
ccimport==0.4.4
cumm-cu121==0.7.11
einops==0.8.0
huggingface-hub==0.36.2
imageio==2.37.4
imageio-ffmpeg==0.6.0
kaolin==0.18.0
numpy==2.2.6
opencv-python-headless==5.0.0.93
plyfile==1.1.5
safetensors==0.8.0
scipy==1.15.3
spconv-cu121==2.3.8
tokenizers==0.20.3
transformers==4.46.3
triton==3.0.0
trimesh==5.1.0
utils3d @ git+https://github.com/EasternJournalist/utils3d.git@9a4eb15e4021b67b12c460c7057d642626897ec
xatlas==0.0.11
xformers==0.0.27.post2

# Native CUDA extension build tools
ninja==1.13.2
pybind11==3.1.0

# Notebook kernel runtime
# Kaolin 0.18.0 requires jupyter-client<8
ipykernel==6.29.5
ipython==8.39.0
jupyter-client==7.4.9

# Project runtime
fastapi==0.141.1
pillow==12.3.0
python-multipart==0.0.32
rembg==2.0.69
requests==2.34.2
tqdm==4.70.1
uvicorn==0.52.4

# Runtime dependency used by background removal
PyMatting==1.1.16
